{% extends 'base.html.twig' %}



{% block body %}
    <h1>Prestations</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ prestation.id }}</td>
            </tr>
            <tr>
                <th>Prestation</th>
                <td>{{ prestation.prestation }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ prestation.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ prestation.image }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ prestation.montant }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_prestations_index') }}">back to list</a>

    <a href="{{ path('app_prestations_edit', {'id': prestation.id}) }}">edit</a>

    {{ include('prestations/_delete_form.html.twig') }}
{% endblock %}
